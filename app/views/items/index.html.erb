<div>
  <% @items.each do |item| %>
    <ul>
      <li>
        <%= link_to "#{item.name}", item_path(item)%>
        <%= link_to "Make a change", edit_item_path(item) %>
      </li>
    </ul>
  <% end %>
</div>
<%= link_to "Add item", new_item_path %>

